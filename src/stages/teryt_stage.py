# TODO (Option D): implement teryt_stage
# run(text) — synchronous, uses Morfeusz2 + TERYT sets from singletons
# Masking policy:
#   SIMC (places):  always mask on match
#   ULIC (streets): always mask on match
#   TERC (admin):   mask only when preceded by an address keyword (woj., pow., gm., ...)
